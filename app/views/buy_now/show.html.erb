<%= render 'layouts/navbar' %>

<h1> <%= @product.name %></h1>
<p> <%= @product.price %> </p>
<p> <%= @product.description %> </p>

<div
  data-controller="stripe-checkout"
  data-stripe-checkout-url-value="<%= product_buy_now_path(product_id: @product.id) %>">
</div>